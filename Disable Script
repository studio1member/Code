using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Disable : MonoBehaviour
{
    public move Move;
    public bool Enable;
    private void Update()
    {
        Move.enabled = Enable;
    }
}
